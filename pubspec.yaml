name: fal_client
description: A client library for fal.ai ML APIs
version: 0.1.0
homepage: fal.ai

environment:
  sdk: '>=3.1.5 <4.0.0'
  flutter: '>=1.17.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  platform: ^3.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
